apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tyk-sre-tool.fullname" . }}-test-connection"
  labels:
    {{- include "tyk-sre-tool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tyk-sre-tool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
